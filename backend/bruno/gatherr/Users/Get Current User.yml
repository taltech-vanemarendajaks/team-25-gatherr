info:
  name: Get Current User
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/users/me"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
